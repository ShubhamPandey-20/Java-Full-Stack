package GitMaven;

public class Git {
    // Git is called version control
    // We can go to any version by the help of the git as it create new version on every changes in our application
    // git init - git add (filename)- git commit m "File added" - git push
    // New branch ---git chekout -b notification

}
